; order of growth to encode the most frequent symbol?
; search the list of symbols once O(n) and returns => O(n)
;
; order of growth to encode the least frequent symbol?
; search the list of symbols once O(n), (n - 1) times => O(n^2)
