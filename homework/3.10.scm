; for each instance
; w1 and w2
; in the "param version" there's a lambda doing the work and an environment binding balance
; in the "let version" there's 2 lambdas and 2 environments
; the first lambda is the "let lambda" and the first environment binds initial-value
; the second lambda does the real work and the second environment binds balance
;
; 
